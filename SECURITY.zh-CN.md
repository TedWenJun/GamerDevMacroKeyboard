# 安全策略

[English](SECURITY.md) | 中文

## 支持的版本

只有最新发布的版本会收到安全修复。

## 报告漏洞

请**不要**通过公开 Issue 报告安全问题。使用 GitHub 仓库的 **Security → Report a vulnerability**（私密漏洞报告）提交，
说明影响、复现步骤和受影响的版本。我们会尽快确认并在修复发布后致谢（如你愿意）。

## 安全模型

MacroHub 是运行在当前用户会话中的本地程序，需要以下能力：

| 能力 | 用途 | 限制 |
|---|---|---|
| 全局低级键盘钩子 | 拦截宏键盘发出的原始按键 | 只对与宏键盘物理报告关联的按键做决定；不记录其他键盘的按键内容 |
| 读取宏键盘 HID 集合 | 获取物理按键身份 | 仅限配置中匹配的设备，只读 |
| 写入宏键盘厂商 Feature 通道 | 设置背光、查询电量 | 只发送灯光写入、应用与只读状态查询三个命令，从不修改键盘的按键映射 |
| SendInput / 运行程序 | 执行用户配置的动作 | 动作只来自用户保存的配置 |
| Web 界面 / API / WebSocket | 配置与测试 | 仅监听 `127.0.0.1`；只接受 Host 为本机、且无 Origin（本地非浏览器客户端）或 Origin 为 MacroHub 自身的请求，防止网页跨站调用和 DNS 重绑定 |
| 命名管道 `\\.\pipe\MacroHub` | 应用接入 | 仅当前 Windows 用户可连接 |

已知边界：

- 同一用户下运行的其他程序可以访问本地 API 和管道（与这些程序本身拥有的权限相同）。配置中的“运行程序”动作因此等同于该用户可执行的命令。
- 以管理员权限运行 MacroHub 时，其执行的动作也具有管理员权限，请仅在需要时使用。
- 发布包目前未进行代码签名。
